#include <stdio.h>

#include "tree.h"

int countNodes(struct tree t) {
    return 0;
}

#ifndef UNIT_TEST
int main() {
    struct tree test;
    
   /* printf() displays the string inside quotation */
   printf("Hello, World!");
   return 0;
}
#endif